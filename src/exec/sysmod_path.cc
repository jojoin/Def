/**
 * 路径处理模块
 */
DO* Exec::SysmodPath(string func, Node* para)
{
    // cout<<"-Exec::SysmodPath-"<<endl;
    LOCALIZE_gc
    NodeGroup* argv = (NodeGroup*) para;
    size_t len = argv->ChildSize();


}